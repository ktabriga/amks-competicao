{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "postgres": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "",
    "database": "${DB_NAME}",
    "password": "${DB_PASSWORD}",
    "name": "postgres",
    "user": "${DB_USER}",
    "connector": "postgresql"
  }
}
